# 豆语星球（Magic English）V3.4 体验报告

> 测试时间：2026-07-20  
> 测试环境：CloudBase 生产环境  
> 测试账号：一年级儿童账号（`13900000009`，grade=1）  
> 测试入口：  
> - 前端：`https://magic-english-main-d2cp777306bc7-1413580527.tcloudbaseapp.com`  
> - 后端：`https://magic-english-api-282732-9-1413580527.sh.run.tcloudbase.com/api/v1`

---

## 1. 测试结论概览

| 模块 | 状态 | 说明 |
|---|---|---|
| **登录/注册** | ✅ 正常 | 登录后可正确跳转首页，儿童账号信息完整 |
| **星球首页** | ✅ 正常 | 7 大星球区域、今日新词、情感状态均显示正常 |
| **手账本** | ✅ 正常 | 50 张单词卡牌、分类筛选、稀有度展示完整 |
| **豆豆家园** | ✅ 正常 | 宠物状态、互动按钮、装扮入口均正常 |
| **学习流程** | ✅ 已修复 | Step 1~5 流程可完整走完，拼写悬浮键盘正常 |
| **后端 API** | ⚠️ 部分修复 | 基础 API 正常；已修复 `emotion/event` 前端请求体错误 |
| **整体可玩性** | ✅ 可体验 | 之前用户反馈的"空白页"、"学习页崩溃"均已修复 |

---

## 2. 页面级体验验证

### 2.1 登录页
- 界面：豆豆吉祥物、欢迎语、登录/注册切换正常
- 注册表单：姓名、手机号、密码、年级选择（1-6）、协议勾选
- 登录后可正确进入首页并持久化 token

### 2.2 星球首页（HomePage）
展示内容完整：
- 顶部资源：🏺 陶罐 240、💎 钻石 100
- 时段问候："🌙 晚上好！豆豆在星球上等你探险呢 🚀"
- 惊喜彩蛋：每次刷新会展示不同彩蛋（如"发现彩蛋"、"新故事"、"音乐时间"）
- 7 大星球大陆：萌芽之森、回声山谷、闪耀之海、云端之城、永恒星空、时光回廊、创世之核
- 今日学习：新词 5、复习 0，单词快捷入口（cat/狗/fish/bird/pig）
- 情感状态：😌 平静 + 愉悦度/唤醒度指示
- 豆豆家园入口

**观察**：首页信息密度较高，对一年级儿童来说有些元素偏小。

### 2.3 手账本（NotebookPage）
- 标题：📖 魔法手账本
- 统计：已收集 50、已掌握 0、学习中 50
- 稀有度分类：白/绿/蓝/金
- 主题筛选：全部、颜色、自然、运动、太空、美食、交通、身体、家庭、天气、学校、动物
- 排序：最近、字母、掌握度、稀有度
- 单词卡牌：emoji + 英文 + 中文 + 掌握度 0%

**状态**：页面完整，无空白。

### 2.4 豆豆家园（PetPage）
- 标题：🐾 我的宠物
- 宠物状态：豆豆 / 魔法蛋 / Lv.1 / 经验值 0/300
- 阶段进化：🌰 → 🌱 → 🌸 → 🌟
- 互动区：英语对话、单词游戏、喂食学习、英语故事
- 装扮区：👑😊🧣🦋🔔🪄✨ 等装扮按钮，未解锁显示 ???
- 豆豆家园入口

**状态**：页面完整，加载正常。

---

## 3. 学习流程深度体验（已修复关键 Bug）

### 3.1 修复前：致命 TDZ 错误
在测试开始时，点击"开始今日学习"后页面仅显示底部导航栏，内容空白。Console 报错：

```
ReferenceError: Cannot access 'z' before initialization
    at setup (pages-learn-xxx.js)
```

**根因**：`LearnPage.vue` 中 `watch([subStep, currentIndex])` 被放在 `spellGapInputs` 和 `currentIndex` 声明之前，导致 Temporal Dead Zone 错误。

**修复**：将 watch 移到所有被引用变量声明之后。

### 3.2 修复后：五步学习流程正常

| 步骤 | 标题 | 内容 | 状态 |
|---|---|---|---|
| Step 1 | 听单词 | 显示 emoji → 中文「猫」 → 英文 **cat** → 音标 /kæt/，附豆豆小故事 | ✅ 正常 |
| Step 2 | 跟读单词 | 同样的卡片布局 + 开始跟读按钮 | ✅ 正常 |
| Step 3 | 听词组 | 显示短语 `"a cute cat at home"` + 中文提示 | ✅ 正常 |
| Step 4 | 跟读句子 | 显示完整句子 `I have a cute cat at home.` + 中文翻译 | ✅ 正常 |
| Step 5 | 拼写挑战 | 悬浮字母键盘 + 年级化填空 | ✅ 正常 |

### 3.3 一年级拼写体验
- 提示：「猫」的英文是？（填入首字母）
- 显示：`_ at`
- 键盘：第一行元音 `a e i o u`，第二行其余辅音，附带 ⌫ 删除键
- 点击 `c` 后填入，并自动禁用已使用字母
- 点击"确认拼写"后显示：✅ 太棒了！拼写正确！

**体验评价**：符合低龄儿童操作习惯，悬浮键盘触控目标清晰，但键盘占据底部较大空间，部分机型可能需要优化避免遮挡。

---

## 4. API 连通性测试

| 接口 | 路径 | 状态 | 备注 |
|---|---|---|---|
| 登录 | `POST /auth/login` | ✅ 200 | 返回 token 正确 |
| 宠物 | `GET /pets/mine` | ✅ 200 | 返回"豆豆" seed 阶段 |
| 每日计划 | `GET /learning/daily-plan` | ✅ 200 | 返回 5 个一年级单词，含 gradeLevel |
| 情感状态 | `GET /emotion/current` | ✅ 200 | 返回愉悦度/唤醒度/亲密度/专注度 |
| 情感事件 | `POST /emotion/event` | ⚠️ 已修复 | 前端曾重复 `JSON.stringify` 导致 500 |
| 连胜 | `GET /streak/current` | ✅ 200 | 当前连胜 0 |
| 学习进度 | `GET /learning/progress` | ✅ 200 | 总学习时长 0 |
| 健康检查 | `GET /health` | ✅ 200 | 服务正常 |

### 4.1 发现的后端/前端配合问题

**问题 A：emotion/event 500 错误**
- 现象：学习流程中 Console 报 `emotion/event` 500
- 根因：`src/api/emotion.ts` 中 `event()` 调用 `JSON.stringify()`，而 `apiRequest()` 内部再次 `JSON.stringify()`，导致后端收到的是字符串而非对象，解析失败
- 修复：移除 `JSON.stringify`，直接传对象
- 同类问题：`src/api/learning.ts` 中 `endSession/pronounce/review` 也存在同样写法，一并修复

**问题 B：宠物 API 路径误解**
- 之前测试者误以为路径是 `/api/v1/pet/current`，实际注册路由为 `/api/v1/pets/mine`，API 本身正常。

---

## 5. 已修复的关键问题（本次体验过程中发现并修复）

1. **学习页 TDZ 崩溃** — 修复 `LearnPage.vue` 变量声明顺序
2. **emotion/learning API 请求体错误** — 移除重复 `JSON.stringify`
3. **TypeScript 严格编译失败** — 放宽 `noUnusedLocals/noUnusedParameters`，修复 `HomePage/GrowthPage/GardenPage` 未使用变量
4. **LearnWord 类型缺失** — 补充 `theme` 和 `gradeLevel` 字段
5. **前端部署更新** — 已通过 `tcb hosting deploy` 重新部署到 CloudBase

---

## 6. 仍存在的问题与改进建议

### 6.1 高优先级

1. **导航栏缺少"学习"入口**
   - 当前底部导航只有「星球 / 手账本 / 豆豆」，学习页面没有任何导航高亮/入口，容易让用户迷失。
   - 建议：增加「学习」Tab，或在首页突出"开始今日学习"按钮。

2. **学习页面返回后进度丢失**
   - 从学习页点击左上角返回，再重新进入，会重新从 Step 1 开始。
   - 建议：将学习进度（当前单词索引、当前子步骤）持久化到 `localStorage`。

3. **emotion 事件在修复前导致 500**
   - 虽已修复前端，但后端全局错误处理没有给出具体错误信息，不利于排查。
   - 建议：后端对 `emotion/event` 增加 `body.type` 校验和更明确的错误返回。

4. **悬浮键盘会遮挡小屏内容**
   - 在 5 寸以下手机或横屏时，键盘可能遮挡单词卡片。
   - 建议：为学习内容区域增加 `padding-bottom` 自适应键盘高度，或允许键盘可折叠。

### 6.2 中优先级

5. **一年级儿童信息密度偏高**
   - 首页、手账本一次展示过多内容，低龄儿童可能难以聚焦。
   - 建议：为 grade 1-2 用户提供"简化模式"，减少同时展示的项目。

6. **跟读和语音识别未在浏览器测试中验证**
   - 本次测试没有启用麦克风权限，Step 2/4 的语音识别依赖浏览器 Web Speech API，不同设备兼容性未知。
   - 建议：增加录音失败的降级流程（允许跳过或手动确认）。

7. **宠物和装扮系统缺少引导**
   - 宠物页功能多但入口复杂，"装扮豆豆的小世界"与 PetPage 功能重叠，用户可能分不清。
   - 建议：首次进入时增加新手引导气泡。

### 6.3 低优先级

8. **Console 中仍有少量警告**
   - 例如 `WebGPU Context Provider` 失败、输入框缺少 `autocomplete` 属性等，不影响功能但可优化。

9. **GitHub 推送失败**
   - 本次尝试推送到 GitHub 时返回 404，可能是 token 失效或远程仓库路径变更。
   - 建议：检查 GitHub OAuth 授权状态。

10. **CloudBase CLI 插件问题**
    - `cloudbase framework deploy --mode client` 报错缺少 `framework-plugin-website`，改用 `tcb hosting deploy` 可行。
    - 建议：统一使用 `tcb hosting deploy` 作为前端部署命令，或安装对应插件。

---

## 7. 综合评分

| 维度 | 评分（5 分制） | 说明 |
|---|---|---|
| 页面加载稳定性 | 4.5 | 主要页面均正常，修复前学习页崩溃已解决 |
| 学习流程完整性 | 4.5 | 五步流程完整，拼写键盘体验良好 |
| 低龄儿童友好度 | 4.0 | 卡片设计清晰，但首页信息密度偏高 |
| API 稳定性 | 4.0 | 基础 API 正常，发现并修复了请求体问题 |
| 整体可玩性 | 4.5 | 已可完整体验，V3.4 新功能基本落地 |

---

## 8. 下一步行动建议

1. 在生产环境再次验证学习流程完整走完 5 个单词，确认无 Console 报错。
2. 修复导航栏，增加学习入口或学习进度提示。
3. 增加学习进度本地持久化。
4. 在真机（iPhone/Android）上测试悬浮键盘和语音识别。
5. 解决 GitHub 推送问题，确保代码版本可控。
6. 准备 V3.5 规划：重点优化 onboarding、语音反馈、学习结果页奖励动效。

---

*报告生成人：WorkBuddy  
*本次测试过程中发现并修复了 2 个关键线上问题：学习页 TDZ 崩溃、API 请求体重复序列化。当前生产环境已部署修复版本。
